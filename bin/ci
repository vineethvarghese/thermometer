#!/bin/sh

$(dirname "$0")/../sbt ';set credentials := Seq(Credentials(file("/etc/sbt.credentials"))) ; set publishTo := Some("commbank-releases" at "http://commbank.artifactoryonline.com/commbank/ext-releases-local"); set publishMavenStyle := true; +publish'
